%h1 Listing messages

%table
  %tr
    %th Sender
    %th Receiver
    %th Content
    %th
    %th
    %th

  - @messages.each do |message|
    %tr
      %td= message.from.email
      %td= message.to.email
      %td= message.body

%br

= link_to t(:new_message), new_message_path